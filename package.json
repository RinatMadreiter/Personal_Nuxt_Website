{
  "name": "personal-nuxt-website",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate && (if exist .output\\public\\NUL (type NUL > .output\\public\\.nojekyll) else (touch .output/public/.nojekyll))",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.0.3",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "sass": "^1.92.0"
  }
}
